//radius of a circle. calculate area and circumfernce
#include<stdio.h>
int main()
{
    float r,pi,area,cir;
    printf("Enter the radius of a circle: ");
    scanf("%f",&r);

    pi=3.1416;
    area=pi*r*r;

    cir=pi*r*2;
  printf("radius area is: %f",area);
    printf("radius circumfernce is: %f",cir);
    getch();

}
void int()
{
    float r,pi,area,cire;
    printf("enter the ")
}
